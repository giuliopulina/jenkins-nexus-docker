* install sshagent Jenkins plugin
* configure Maven tool in Jenkins and call it M3
* setup credentials with private key for git in Jenkins
* setup credentials with Nexus with username and password

* create Pipeline Jenkins job (Sample Java Library release) referencing maven_release_JenkinsFile with one parameter 
	git_project = git@github.com:giuliopulina/sample-java-library.git

Nexus

* create sample-releases maven repository(maybe decide a better name :P)
* 